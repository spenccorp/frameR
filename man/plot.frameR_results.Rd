% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/measure.R, R/plot-Desktop.R, R/plot.R
\name{plot.frameR_results}
\alias{plot.frameR_results}
\title{Plot frame similarity results}
\usage{
\method{plot}{frameR_results}(x, type = "all", ...)

\method{plot}{frameR_results}(x, type = "all", ...)

\method{plot}{frameR_results}(x, type = "all", ...)
}
\arguments{
\item{x}{A \code{frameR_results} object returned by
\code{\link{measure_frames}}.}

\item{type}{Character. One of \code{"all"}, \code{"similarity"},
\code{"ratios"}, or \code{"differences"}. Default \code{"all"}.}

\item{...}{Additional arguments (ignored).}
}
\value{
Called for its side effects. Produces a plot.

Called for its side effects. Produces a plot.

Called for its side effects. Produces a plot.
}
\description{
Produces a set of diagnostic and results plots for a
\code{frameR_results} object. By default produces three panels:
absolute frame similarity by period, relative frame emphasis
by period, and between-period differences with significance.

Produces a set of diagnostic and results plots for a
\code{frameR_results} object. By default produces three panels:
absolute frame similarity by period, relative frame emphasis
by period, and between-period differences with significance.

Produces a set of diagnostic and results plots for a
\code{frameR_results} object. By default produces three panels:
absolute frame similarity by period, relative frame emphasis
by period, and between-period differences with significance.
}
\examples{
\dontrun{
results <- measure_frames(...)
plot(results)
plot(results, type = "differences")
}

\dontrun{
results <- measure_frames(...)
plot(results)
plot(results, type = "differences")
}

\dontrun{
results <- measure_frames(...)
plot(results)
plot(results, type = "differences")
}

}
